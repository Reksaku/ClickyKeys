<Window
    x:Class="ClickyKeys.Settings"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="clr-namespace:ClickyKeys"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
    mc:Ignorable="d"
    Closing="Window_Closed"
    Title="Settings" 
    Height="580" MinHeight="240" MaxHeight="750"
    Width="440" MinWidth="440" MaxWidth="440"
    Icon="/Resources/Icon.ico">
    

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>


        <Label Content="Settings" HorizontalAlignment="Center" VerticalAlignment="Top" FontFamily="Futura Bk BT" FontSize="28" Grid.Row="0"/>

        <ScrollViewer Grid.Row="1">
        <Grid >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="20"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="20"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="20"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="20"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="20"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="20"/>
            </Grid.RowDefinitions>

            <Label Content="Grid rows" HorizontalAlignment="Center" Margin="30,0,30,0" VerticalAlignment="Center" Grid.Row="0" Grid.Column="0" FontFamily="Futura Bk BT" FontSize="18" x:Name="RowsLabel"/>
            <materialDesign:DecimalUpDown HorizontalAlignment="Left" VerticalAlignment="Center" Margin="20,0,0,0" Width="100" Grid.Row="0" Grid.Column="1" Minimum="1" Maximum="10" FontSize="16" x:Name="RowsCount"/>
            <Button Content="Set" HorizontalAlignment="Left" Margin="140,0,0,0"  VerticalAlignment="Center" Grid.Row="0" Grid.Column="1" Click="Click_GridRows"/>

            
            <Label Content="Grid columns" HorizontalAlignment="Center" Margin="30,0,30,0" VerticalAlignment="Center" Grid.Row="2" Grid.Column="0" FontFamily="Futura Bk BT" FontSize="18" x:Name="ColumnsLabel"/>
            <materialDesign:DecimalUpDown HorizontalAlignment="Left" VerticalAlignment="Center" Margin="20,0,0,0" Width="100" Grid.Row="2" Grid.Column="1" Minimum="1" Maximum="10" FontSize="16" x:Name="ColumnsCount"/>
            <Button Content="Set" HorizontalAlignment="Left" Margin="140,0,0,0"  VerticalAlignment="Center" Grid.Row="2" Grid.Column="1" Click="Click_GridColumns"/>

            
            <Label Content="Background color" HorizontalAlignment="Center" Margin="30,0,30,0" VerticalAlignment="Center" Grid.Row="4" Grid.Column="0" Grid.RowSpan="2" FontFamily="Futura Bk BT" FontSize="18"/>
            <CheckBox x:Name="BackgroundPalletCheckBox" Content="Expand pallet" HorizontalAlignment="Center" Margin="0,0,0,-5" VerticalAlignment="Top" Width="100" Grid.Row="4" Grid.Column="1"/>
            <materialDesign:ColorPicker x:Name="BackgroundColorPicker"
                HorizontalAlignment="Center" VerticalAlignment="Center"
                Width="200" Grid.Row="5" Grid.Column="1">
                <materialDesign:ColorPicker.Style>
                    <Style TargetType="materialDesign:ColorPicker">
                       
                        <Setter Property="Height" Value="36" />

                        <Style.Triggers>
                            <DataTrigger Value="True">
                                <DataTrigger.Binding>
                                    <Binding Path="IsChecked"
                        Source="{x:Reference BackgroundPalletCheckBox}" />
                                </DataTrigger.Binding>

                                
                                <DataTrigger.EnterActions>
                                    <BeginStoryboard x:Name="ExpandHeightStoryboard" HandoffBehavior="SnapshotAndReplace">
                                        <Storyboard>
                                            <DoubleAnimation
                                    Storyboard.TargetProperty="Height"
                                    To="80"
                                    Duration="0:0:0.20">
                                                <DoubleAnimation.EasingFunction>
                                                    <QuadraticEase EasingMode="EaseOut"/>
                                                </DoubleAnimation.EasingFunction>
                                            </DoubleAnimation>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </DataTrigger.EnterActions>

                                
                                <DataTrigger.ExitActions>
                                    <BeginStoryboard x:Name="CollapseHeightStoryboard" HandoffBehavior="SnapshotAndReplace">
                                        <Storyboard>
                                            <DoubleAnimation
                                    Storyboard.TargetProperty="Height"
                                    To="36"
                                    Duration="0:0:0.20">
                                                <DoubleAnimation.EasingFunction>
                                                    <QuadraticEase EasingMode="EaseIn"/>
                                                </DoubleAnimation.EasingFunction>
                                            </DoubleAnimation>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </DataTrigger.ExitActions>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </materialDesign:ColorPicker.Style>
            </materialDesign:ColorPicker>

            
            <Label Content="Panels color" HorizontalAlignment="Center" Margin="30,0,30,0" VerticalAlignment="Center" Grid.Row="7" Grid.Column="0" Grid.RowSpan="2" FontFamily="Futura Bk BT" FontSize="18"/>
            <CheckBox x:Name="PanelsPalletCheckBox" Content="Expand pallet" HorizontalAlignment="Center" Margin="0,0,0,-5" VerticalAlignment="Top" Width="100" Grid.Row="7" Grid.Column="1"/>
            <materialDesign:ColorPicker x:Name="PanelsColorPicker"
                HorizontalAlignment="Center" VerticalAlignment="Center"
                Width="200" Grid.Row="8" Grid.Column="1">
                <materialDesign:ColorPicker.Style>
                    <Style TargetType="materialDesign:ColorPicker">
                        
                        <Setter Property="Height" Value="36" />

                        <Style.Triggers>
                            <DataTrigger Value="True">
                                <DataTrigger.Binding>
                                    <Binding Path="IsChecked"
                        Source="{x:Reference PanelsPalletCheckBox}" />
                                </DataTrigger.Binding>

                                
                                <DataTrigger.EnterActions>
                                    <BeginStoryboard x:Name="ExpandHeightStoryboard" HandoffBehavior="SnapshotAndReplace">
                                        <Storyboard>
                                            <DoubleAnimation
                                    Storyboard.TargetProperty="Height"
                                    To="80"
                                    Duration="0:0:0.20">
                                                <DoubleAnimation.EasingFunction>
                                                    <QuadraticEase EasingMode="EaseOut"/>
                                                </DoubleAnimation.EasingFunction>
                                            </DoubleAnimation>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </DataTrigger.EnterActions>

                                
                                <DataTrigger.ExitActions>
                                    <BeginStoryboard x:Name="CollapseHeightStoryboard" HandoffBehavior="SnapshotAndReplace">
                                        <Storyboard>
                                            <DoubleAnimation
                                    Storyboard.TargetProperty="Height"
                                    To="36"
                                    Duration="0:0:0.20">
                                                <DoubleAnimation.EasingFunction>
                                                    <QuadraticEase EasingMode="EaseIn"/>
                                                </DoubleAnimation.EasingFunction>
                                            </DoubleAnimation>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </DataTrigger.ExitActions>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </materialDesign:ColorPicker.Style>
            </materialDesign:ColorPicker>
            
            
            <Label Content="Keys text color" HorizontalAlignment="Center" Margin="30,0,30,0" VerticalAlignment="Center" Grid.Row="10" Grid.Column="0" Grid.RowSpan="2" FontFamily="Futura Bk BT" FontSize="18"/>
            <CheckBox x:Name="KeyssPalletCheckBox" Content="Expand pallet" HorizontalAlignment="Center" Margin="0,0,0,-5" VerticalAlignment="Top" Width="100" Grid.Row="10" Grid.Column="1"/>
            <materialDesign:ColorPicker x:Name="KeysColorPicker"
    HorizontalAlignment="Center" VerticalAlignment="Center"
    Width="200" Grid.Row="11" Grid.Column="1">
                <materialDesign:ColorPicker.Style>
                    <Style TargetType="materialDesign:ColorPicker">
                        
                        <Setter Property="Height" Value="36" />

                        <Style.Triggers>
                            <DataTrigger Value="True">
                                <DataTrigger.Binding>
                                    <Binding Path="IsChecked"
                                    Source="{x:Reference KeyssPalletCheckBox}" />
                                </DataTrigger.Binding>

                                
                                <DataTrigger.EnterActions>
                                    <BeginStoryboard x:Name="ExpandHeightStoryboard" HandoffBehavior="SnapshotAndReplace">
                                        <Storyboard>
                                            <DoubleAnimation
                                            Storyboard.TargetProperty="Height"
                                            To="80"
                                            Duration="0:0:0.20">
                                                <DoubleAnimation.EasingFunction>
                                                    <QuadraticEase EasingMode="EaseOut"/>
                                                </DoubleAnimation.EasingFunction>
                                            </DoubleAnimation>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </DataTrigger.EnterActions>

                                
                                <DataTrigger.ExitActions>
                                    <BeginStoryboard x:Name="CollapseHeightStoryboard" HandoffBehavior="SnapshotAndReplace">
                                        <Storyboard>
                                            <DoubleAnimation
                                            Storyboard.TargetProperty="Height"
                                            To="36"
                                            Duration="0:0:0.20">
                                                <DoubleAnimation.EasingFunction>
                                                    <QuadraticEase EasingMode="EaseIn"/>
                                                </DoubleAnimation.EasingFunction>
                                            </DoubleAnimation>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </DataTrigger.ExitActions>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </materialDesign:ColorPicker.Style>
            </materialDesign:ColorPicker>


            <Label Content="Values text color" HorizontalAlignment="Center" Margin="30,0,30,0" VerticalAlignment="Center" Grid.Row="13" Grid.Column="0" Grid.RowSpan="2" FontFamily="Futura Bk BT" FontSize="18"/>
            <CheckBox x:Name="ValuessPalletCheckBox" Content="Expand pallet" HorizontalAlignment="Center" Margin="0,0,0,-5" VerticalAlignment="Top" Width="100" Grid.Row="13" Grid.Column="1"/>
            <materialDesign:ColorPicker x:Name="ValuesColorPicker"
HorizontalAlignment="Center" VerticalAlignment="Center"
Width="200" Grid.Row="14" Grid.Column="1">
                <materialDesign:ColorPicker.Style>
                    <Style TargetType="materialDesign:ColorPicker">
                        
                        <Setter Property="Height" Value="36" />

                        <Style.Triggers>
                            <DataTrigger Value="True">
                                <DataTrigger.Binding>
                                    <Binding Path="IsChecked"
                                Source="{x:Reference ValuessPalletCheckBox}" />
                                </DataTrigger.Binding>

                                
                                <DataTrigger.EnterActions>
                                    <BeginStoryboard x:Name="ExpandHeightStoryboard" HandoffBehavior="SnapshotAndReplace">
                                        <Storyboard>
                                            <DoubleAnimation
                                        Storyboard.TargetProperty="Height"
                                        To="80"
                                        Duration="0:0:0.20">
                                                <DoubleAnimation.EasingFunction>
                                                    <QuadraticEase EasingMode="EaseOut"/>
                                                </DoubleAnimation.EasingFunction>
                                            </DoubleAnimation>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </DataTrigger.EnterActions>

                                
                                <DataTrigger.ExitActions>
                                    <BeginStoryboard x:Name="CollapseHeightStoryboard" HandoffBehavior="SnapshotAndReplace">
                                        <Storyboard>
                                            <DoubleAnimation
                                        Storyboard.TargetProperty="Height"
                                        To="36"
                                        Duration="0:0:0.20">
                                                <DoubleAnimation.EasingFunction>
                                                    <QuadraticEase EasingMode="EaseIn"/>
                                                </DoubleAnimation.EasingFunction>
                                            </DoubleAnimation>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </DataTrigger.ExitActions>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </materialDesign:ColorPicker.Style>
            </materialDesign:ColorPicker>
        </Grid>
        </ScrollViewer>

        <Grid HorizontalAlignment="Right" VerticalAlignment="Bottom" Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Button Content="Save" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,10,10,10" Grid.Column="0" Click="Click_SaveAndClose"/>
            <Button Content="Exit" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,10,10" Grid.Column="1" Click="Click_Close"/>
        </Grid>


    </Grid>

</Window>
