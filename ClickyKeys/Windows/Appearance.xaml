<Window
    x:Class="ClickyKeys.Appearance"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="clr-namespace:ClickyKeys"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
    xmlns:controls="clr-namespace:ClickyKeys.Controls"
    mc:Ignorable="d"
    Closing="Window_Closed"
    Title="{DynamicResource Appearance_Title}"
    Height="885" MinHeight="240" MaxHeight="1045"
    Width="460" MinWidth="460" MaxWidth="460"
    Icon="/Resources/Icon.ico">

    <Window.Resources>
        <Style x:Key="PlaceholderTextBox" TargetType="TextBox">
            <Setter Property="Padding" Value="4,2,4,2"/>

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Grid>
                            <!-- Text -->
                            <ScrollViewer x:Name="PART_ContentHost"
                                  Margin="{TemplateBinding Padding}"
                                  VerticalAlignment="Center"/>

                            <!-- Placeholder -->
                            <TextBlock x:Name="Placeholder"
                               Text="{TemplateBinding Tag}"
                               Margin="{TemplateBinding Padding}"
                               Foreground="Gray"
                               VerticalAlignment="Center"
                               IsHitTestVisible="False"
                               Visibility="Collapsed"/>
                        </Grid>

                        <ControlTemplate.Triggers>
                            <!-- Placeholder visibility -->
                            <Trigger Property="Text" Value="">
                                <Setter TargetName="Placeholder"
                                Property="Visibility"
                                Value="Visible" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>



    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="42"/>
            <RowDefinition Height="33"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="52"/>
        </Grid.RowDefinitions>


        <Label x:Name="AppearanceLabel" Content="{DynamicResource Appearance_Title}" HorizontalAlignment="Center" VerticalAlignment="Top" FontFamily="Futura Bk BT" FontSize="28" Grid.Row="0"/>
        <Label x:Name="ProfilesLabel" Content="{DynamicResource Appearance_Profile}" HorizontalAlignment="Center" VerticalAlignment="Top" FontFamily="Futura Bk BT" FontSize="20" Grid.Row="1"/>
        <ScrollViewer Grid.Row="2">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel x:Name="GridSizeScetion" Grid.Row="0">

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="20"/>
                        </Grid.RowDefinitions>

                        <Label Content="{DynamicResource Appearance_GridRows}" HorizontalAlignment="Center" Margin="30,0,30,0" VerticalAlignment="Center" Grid.Row="0" Grid.Column="0" FontFamily="Futura Bk BT" FontSize="18" x:Name="RowsLabel"/>
                        <materialDesign:DecimalUpDown HorizontalAlignment="Left" VerticalAlignment="Center" Margin="20,0,0,0" Width="100" Grid.Row="0" Grid.Column="1" Minimum="1" Maximum="10" FontSize="16" x:Name="RowsCount"/>
                        <Button Content="{DynamicResource Common_Set}" HorizontalAlignment="Left" Margin="140,0,0,0"  VerticalAlignment="Center" Grid.Row="0" Grid.Column="1" Click="Click_GridRows"/>
                    </Grid>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="20"/>
                        </Grid.RowDefinitions>
                        <Label Content="{DynamicResource Appearance_GridColumns}" HorizontalAlignment="Center" Margin="30,0,30,0" VerticalAlignment="Center" Grid.Row="0" Grid.Column="0" FontFamily="Futura Bk BT" FontSize="18" x:Name="ColumnsLabel"/>
                        <materialDesign:DecimalUpDown HorizontalAlignment="Left" VerticalAlignment="Center" Margin="20,0,0,0" Width="100" Grid.Row="0" Grid.Column="1" Minimum="1" Maximum="10" FontSize="16" x:Name="ColumnsCount"/>
                        <Button Content="{DynamicResource Common_Set}" HorizontalAlignment="Left" Margin="140,0,0,0"  VerticalAlignment="Center" Grid.Row="0" Grid.Column="1" Click="Click_GridColumns"/>
                    </Grid>
                </StackPanel>

                <StackPanel x:Name="ColorsSection" Grid.Row="1">

                    <Label Content="{DynamicResource Appearance_Colors}" HorizontalAlignment="Center" VerticalAlignment="Top" FontFamily="Futura Bk BT" FontSize="16" Grid.Row="0"/>

                    <Grid x:Name="BackgroundGrid">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="20"/>
                        </Grid.RowDefinitions>

                        <Label Content="{DynamicResource Appearance_BackgroundColor}" HorizontalAlignment="Center" Margin="10,0,10,0" VerticalAlignment="Center" Grid.Row="0" Grid.Column="0" Grid.RowSpan="2" FontFamily="Futura Bk BT" FontSize="18" Width="Auto"/>
                        <CheckBox x:Name="BackgroundPalletCheckBox" Content="{DynamicResource Appearance_ExpandPallet}" HorizontalAlignment="Center" Margin="0,0,0,-5" VerticalAlignment="Top" Width="Auto" Grid.Row="0" Grid.Column="1"/>
                        <materialDesign:ColorPicker x:Name="BackgroundColorPicker"
     HorizontalAlignment="Center" VerticalAlignment="Center"
     Width="200" Grid.Row="1" Grid.Column="1">
                            <materialDesign:ColorPicker.Style>
                                <Style TargetType="materialDesign:ColorPicker">

                                    <Setter Property="Height" Value="36" />

                                    <Style.Triggers>
                                        <DataTrigger Value="True">
                                            <DataTrigger.Binding>
                                                <Binding Path="IsChecked"
             Source="{x:Reference BackgroundPalletCheckBox}" />
                                            </DataTrigger.Binding>


                                            <DataTrigger.EnterActions>
                                                <BeginStoryboard x:Name="ExpandHeightStoryboard" HandoffBehavior="SnapshotAndReplace">
                                                    <Storyboard>
                                                        <DoubleAnimation
                         Storyboard.TargetProperty="Height"
                         To="80"
                         Duration="0:0:0.20">
                                                            <DoubleAnimation.EasingFunction>
                                                                <QuadraticEase EasingMode="EaseOut"/>
                                                            </DoubleAnimation.EasingFunction>
                                                        </DoubleAnimation>
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </DataTrigger.EnterActions>


                                            <DataTrigger.ExitActions>
                                                <BeginStoryboard x:Name="CollapseHeightStoryboard" HandoffBehavior="SnapshotAndReplace">
                                                    <Storyboard>
                                                        <DoubleAnimation
                         Storyboard.TargetProperty="Height"
                         To="36"
                         Duration="0:0:0.20">
                                                            <DoubleAnimation.EasingFunction>
                                                                <QuadraticEase EasingMode="EaseIn"/>
                                                            </DoubleAnimation.EasingFunction>
                                                        </DoubleAnimation>
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </DataTrigger.ExitActions>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </materialDesign:ColorPicker.Style>
                        </materialDesign:ColorPicker>
                    </Grid>

                    <Grid x:Name="PanelsdGrid">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="20"/>
                        </Grid.RowDefinitions>
                        <Label Content="{DynamicResource Appearance_PanelsColor}" HorizontalAlignment="Center" Margin="10,0,10,0" VerticalAlignment="Center" Grid.Row="0" Grid.Column="0" Grid.RowSpan="2" FontFamily="Futura Bk BT" FontSize="18" Width="Auto"/>
                        <CheckBox x:Name="PanelsPalletCheckBox" Content="{DynamicResource Appearance_ExpandPallet}" HorizontalAlignment="Center" Margin="0,0,0,-5" VerticalAlignment="Top" Width="Auto" Grid.Row="0" Grid.Column="1"/>
                        <materialDesign:ColorPicker x:Name="PanelsColorPicker"
    HorizontalAlignment="Center" VerticalAlignment="Center"
    Width="200" Grid.Row="1" Grid.Column="1">
                            <materialDesign:ColorPicker.Style>
                                <Style TargetType="materialDesign:ColorPicker">

                                    <Setter Property="Height" Value="36" />

                                    <Style.Triggers>
                                        <DataTrigger Value="True">
                                            <DataTrigger.Binding>
                                                <Binding Path="IsChecked"
            Source="{x:Reference PanelsPalletCheckBox}" />
                                            </DataTrigger.Binding>


                                            <DataTrigger.EnterActions>
                                                <BeginStoryboard x:Name="ExpandHeightStoryboard" HandoffBehavior="SnapshotAndReplace">
                                                    <Storyboard>
                                                        <DoubleAnimation
                        Storyboard.TargetProperty="Height"
                        To="80"
                        Duration="0:0:0.20">
                                                            <DoubleAnimation.EasingFunction>
                                                                <QuadraticEase EasingMode="EaseOut"/>
                                                            </DoubleAnimation.EasingFunction>
                                                        </DoubleAnimation>
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </DataTrigger.EnterActions>


                                            <DataTrigger.ExitActions>
                                                <BeginStoryboard x:Name="CollapseHeightStoryboard" HandoffBehavior="SnapshotAndReplace">
                                                    <Storyboard>
                                                        <DoubleAnimation
                        Storyboard.TargetProperty="Height"
                        To="36"
                        Duration="0:0:0.20">
                                                            <DoubleAnimation.EasingFunction>
                                                                <QuadraticEase EasingMode="EaseIn"/>
                                                            </DoubleAnimation.EasingFunction>
                                                        </DoubleAnimation>
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </DataTrigger.ExitActions>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </materialDesign:ColorPicker.Style>
                        </materialDesign:ColorPicker>
                    </Grid>

                    <Grid x:Name="KeysdGrid">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="20"/>
                        </Grid.RowDefinitions>

                        <Label Content="{DynamicResource Appearance_KeysTextColor}" HorizontalAlignment="Center" Margin="10,0,10,0" VerticalAlignment="Center" Grid.Row="0" Grid.Column="0" Grid.RowSpan="2" FontFamily="Futura Bk BT" FontSize="18" Width="Auto"/>
                        <CheckBox x:Name="KeyssPalletCheckBox" Content="{DynamicResource Appearance_ExpandPallet}" HorizontalAlignment="Center" Margin="0,0,0,-5" VerticalAlignment="Top" Width="Auto" Grid.Row="0" Grid.Column="1"/>
                        <materialDesign:ColorPicker x:Name="KeysColorPicker"
    HorizontalAlignment="Center" VerticalAlignment="Center"
    Width="200" Grid.Row="1" Grid.Column="1">
                            <materialDesign:ColorPicker.Style>
                                <Style TargetType="materialDesign:ColorPicker">

                                    <Setter Property="Height" Value="36" />

                                    <Style.Triggers>
                                        <DataTrigger Value="True">
                                            <DataTrigger.Binding>
                                                <Binding Path="IsChecked"
                                    Source="{x:Reference KeyssPalletCheckBox}" />
                                            </DataTrigger.Binding>


                                            <DataTrigger.EnterActions>
                                                <BeginStoryboard x:Name="ExpandHeightStoryboard" HandoffBehavior="SnapshotAndReplace">
                                                    <Storyboard>
                                                        <DoubleAnimation
                                            Storyboard.TargetProperty="Height"
                                            To="80"
                                            Duration="0:0:0.20">
                                                            <DoubleAnimation.EasingFunction>
                                                                <QuadraticEase EasingMode="EaseOut"/>
                                                            </DoubleAnimation.EasingFunction>
                                                        </DoubleAnimation>
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </DataTrigger.EnterActions>


                                            <DataTrigger.ExitActions>
                                                <BeginStoryboard x:Name="CollapseHeightStoryboard" HandoffBehavior="SnapshotAndReplace">
                                                    <Storyboard>
                                                        <DoubleAnimation
                                            Storyboard.TargetProperty="Height"
                                            To="36"
                                            Duration="0:0:0.20">
                                                            <DoubleAnimation.EasingFunction>
                                                                <QuadraticEase EasingMode="EaseIn"/>
                                                            </DoubleAnimation.EasingFunction>
                                                        </DoubleAnimation>
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </DataTrigger.ExitActions>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </materialDesign:ColorPicker.Style>
                        </materialDesign:ColorPicker>
                    </Grid>

                    <Grid x:Name="ValuesdGrid">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="20"/>
                        </Grid.RowDefinitions>
                        <Label Content="{DynamicResource Appearance_ValuesTextColor}" HorizontalAlignment="Center" Margin="10,0,10,0" VerticalAlignment="Center" Grid.Row="0" Grid.Column="0" Grid.RowSpan="2" FontFamily="Futura Bk BT" FontSize="18" Width="Auto"/>
                        <CheckBox x:Name="ValuessPalletCheckBox" Content="{DynamicResource Appearance_ExpandPallet}" HorizontalAlignment="Center" Margin="0,0,0,-5" VerticalAlignment="Top" Width="Auto" Grid.Row="0" Grid.Column="1"/>
                        <materialDesign:ColorPicker x:Name="ValuesColorPicker"
HorizontalAlignment="Center" VerticalAlignment="Center"
Width="200" Grid.Row="1" Grid.Column="1">
                            <materialDesign:ColorPicker.Style>
                                <Style TargetType="materialDesign:ColorPicker">

                                    <Setter Property="Height" Value="36" />

                                    <Style.Triggers>
                                        <DataTrigger Value="True">
                                            <DataTrigger.Binding>
                                                <Binding Path="IsChecked"
                                Source="{x:Reference ValuessPalletCheckBox}" />
                                            </DataTrigger.Binding>


                                            <DataTrigger.EnterActions>
                                                <BeginStoryboard x:Name="ExpandHeightStoryboard" HandoffBehavior="SnapshotAndReplace">
                                                    <Storyboard>
                                                        <DoubleAnimation
                                        Storyboard.TargetProperty="Height"
                                        To="80"
                                        Duration="0:0:0.20">
                                                            <DoubleAnimation.EasingFunction>
                                                                <QuadraticEase EasingMode="EaseOut"/>
                                                            </DoubleAnimation.EasingFunction>
                                                        </DoubleAnimation>
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </DataTrigger.EnterActions>


                                            <DataTrigger.ExitActions>
                                                <BeginStoryboard x:Name="CollapseHeightStoryboard" HandoffBehavior="SnapshotAndReplace">
                                                    <Storyboard>
                                                        <DoubleAnimation
                                        Storyboard.TargetProperty="Height"
                                        To="36"
                                        Duration="0:0:0.20">
                                                            <DoubleAnimation.EasingFunction>
                                                                <QuadraticEase EasingMode="EaseIn"/>
                                                            </DoubleAnimation.EasingFunction>
                                                        </DoubleAnimation>
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </DataTrigger.ExitActions>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </materialDesign:ColorPicker.Style>
                        </materialDesign:ColorPicker>
                    </Grid>
                    <Label Content="{DynamicResource Appearance_SaturationNote}" Margin="0,-10,0,0" HorizontalAlignment="Center" VerticalAlignment="Top" FontFamily="Futura Bk BT" FontSize="10"/>
                </StackPanel>

                <StackPanel x:Name="FontsSection" Grid.Row="2">

                    <Grid x:Name="KeysFontGrid">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="20"/>
                        </Grid.RowDefinitions>
                        <Label Content="{DynamicResource Appearance_KeysFont}" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,15,0,0" FontFamily="Futura Bk BT" FontSize="16" Grid.Row="0"/>
                        <controls:FontPicker Grid.Row="1" x:Name="KeysFontPicker"/>
                    </Grid>

                    <Grid x:Name="ValuesFontGrid">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="20"/>
                        </Grid.RowDefinitions>

                        <Label Content="{DynamicResource Appearance_ValuesFont}" HorizontalAlignment="Center" VerticalAlignment="Top" FontFamily="Futura Bk BT" FontSize="16" Grid.Row="0"/>
                        <controls:FontPicker Grid.Row="1" x:Name="ValuesFontPicker"/>
                    </Grid>

                </StackPanel>

                <StackPanel x:Name="RainbowSection" Grid.Row="3">
                    <Label Content="{DynamicResource Appearance_RainbowBackground}" HorizontalAlignment="Center" Margin="0,10,0,0" VerticalAlignment="Top" FontFamily="Futura Bk BT" FontSize="16" Grid.Row="0"/>


                    <CheckBox x:Name="BackgroundRainbowCheckBox" Click="Click_BackgroundRainbowCheckBox" Content="{DynamicResource Appearance_RainbowBackgroundMode}" HorizontalAlignment="Center" Margin="0,0,0,0" VerticalAlignment="Top"/>

                </StackPanel>

            </Grid>
        </ScrollViewer>

        <Grid VerticalAlignment="Bottom" Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Button Content="{DynamicResource Common_Load}" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="10,10,10,10" Grid.Column="0" Click="Click_Load"/>
            <Button x:Name="SaveAsButton" Content="{DynamicResource Common_SaveAs}" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,10,10,10" Grid.Column="2" Click="Click_SaveAs"/>
            <Button Content="{DynamicResource Common_Save}" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,10,10,10" Grid.Column="3" Click="Click_SaveAndClose"/>
            <Button Content="{DynamicResource Common_Exit}" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,10,10" Grid.Column="4" Click="Click_Close"/>

        </Grid>

        <!-- "Save As" popup: a modal in-window overlay where the user types the
             new profile name and explicitly confirms or cancels. Spans every
             row so it dims the whole window; collapsed until SaveAs is clicked.
             Replaces the previous inline textbox-in-the-footer flow. -->
        <Grid x:Name="SaveAsOverlay" Grid.Row="0" Grid.RowSpan="4"
              Panel.ZIndex="10" Visibility="Collapsed" Background="#80000000">
            <materialDesign:Card Width="320" Padding="16"
                                 HorizontalAlignment="Center" VerticalAlignment="Center">
                <StackPanel>
                    <TextBlock Text="{DynamicResource Appearance_SaveAsTitle}"
                               FontWeight="Bold" FontSize="16" Margin="0,0,0,12"/>
                    <Border BorderBrush="#BDBDBD" BorderThickness="1" CornerRadius="3" Padding="4,2,4,2">
                        <TextBox x:Name="SaveAsNameTextBox"
                                 Style="{StaticResource PlaceholderTextBox}"
                                 Tag="{DynamicResource Appearance_SaveAsPlaceholder}"
                                 MaxLength="30" TextWrapping="NoWrap"
                                 KeyDown="SaveAsNameTextBox_KeyDown"/>
                    </Border>
                    <TextBlock x:Name="SaveAsErrorText"
                               Text="{DynamicResource Appearance_SaveAsEmptyError}"
                               Foreground="#D32F2F" FontSize="11"
                               Margin="2,4,0,0" Visibility="Collapsed"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
                        <Button Content="{DynamicResource Common_Cancel}"
                                Style="{StaticResource MaterialDesignFlatButton}"
                                Margin="0,0,8,0" Click="Click_SaveAsCancel"/>
                        <Button Content="{DynamicResource Common_Save}"
                                Style="{StaticResource MaterialDesignRaisedButton}"
                                Click="Click_SaveAsConfirm"/>
                    </StackPanel>
                </StackPanel>
            </materialDesign:Card>
        </Grid>

    </Grid>

</Window>