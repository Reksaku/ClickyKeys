<Window x:Class="ClickyKeys.Windows.AlreadyRunningToast"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="340" Height="80"
        WindowStyle="None"
        ResizeMode="NoResize"
        WindowStartupLocation="Manual"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        Topmost="True"
        IsHitTestVisible="False">

    <Window.Triggers>
        <!-- Fade in on load -->
        <EventTrigger RoutedEvent="Window.Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                     From="0" To="1" Duration="0:0:0.25"/>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </Window.Triggers>

    <Border Background="#F01A1A2E"
            CornerRadius="10">
        <StackPanel Orientation="Horizontal"
                    VerticalAlignment="Center"
                    Margin="20,0">
            <Image Source="/Resources/Icon.ico"
                   Width="28" Height="28"
                   VerticalAlignment="Center"
                   Margin="0,0,14,0"/>
            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="ClickyKeys"
                           FontSize="14"
                           FontWeight="SemiBold"
                           Foreground="White"/>
                <TextBlock Text="{DynamicResource Toast_AlreadyRunning}"
                           FontSize="12"
                           Foreground="#BBBBBB"/>
            </StackPanel>
        </StackPanel>
    </Border>

</Window>
