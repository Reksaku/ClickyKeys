<Window
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    x:Class="ClickyKeys.TutorialWindow"
    Title="ClickyKeys Tutorial"
    WindowStyle="None"
    AllowsTransparency="True"
    Background="Transparent"
    ShowInTaskbar="False"
    Topmost="True"
    ResizeMode="NoResize"
    SizeToContent="WidthAndHeight"
    IsHitTestVisible="True">

    <!--
        Floating tutorial card — no full-screen overlay so the user can
        interact with the main window panels at any point during the tutorial.
        The card positions itself near the highlighted element via code-behind.
    -->

    <Border x:Name="CardBorder"
            Background="White"
            CornerRadius="10"
            Padding="18,14"
            Width="310"
            Margin="8">
        <Border.Effect>
            <DropShadowEffect BlurRadius="18" ShadowDepth="3"
                              Opacity="0.28" Color="Black"/>
        </Border.Effect>

        <StackPanel>

            <!-- Step indicator -->
            <TextBlock x:Name="StepIndicator"
                       FontSize="11"
                       Foreground="#888"
                       HorizontalAlignment="Right"
                       Margin="0,0,0,4"/>

            <!-- Optional title -->
            <TextBlock x:Name="TitleText"
                       FontSize="14"
                       FontWeight="SemiBold"
                       TextWrapping="Wrap"
                       Margin="0,0,0,6"
                       Visibility="Collapsed"/>

            <!-- Body copy -->
            <TextBlock x:Name="BodyText"
                       FontSize="12"
                       TextWrapping="Wrap"
                       LineHeight="18"
                       Margin="0,0,0,12"/>

            <!-- Arrow hint — shown when pointing at a specific element -->
            <TextBlock x:Name="ArrowHint"
                       FontSize="11"
                       Foreground="#1976D2"
                       FontStyle="Italic"
                       TextWrapping="Wrap"
                       Margin="0,0,0,10"
                       Visibility="Collapsed"/>

            <!-- Progress bar -->
            <ProgressBar x:Name="ProgressBar"
                         Height="3"
                         Minimum="0"
                         Margin="0,0,0,12"
                         Foreground="#1976D2"
                         Background="#E0E0E0"/>

            <!-- Navigation -->
            <StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Right">
                <Button x:Name="SkipBtn"
                        Content="Skip"
                        Padding="10,4"
                        Margin="0,0,8,0"
                        Click="Skip_Click"
                        Foreground="#888"/>
                <Button x:Name="BackBtn"
                        Content="◀ Back"
                        Padding="10,4"
                        Margin="0,0,8,0"
                        Click="Back_Click"
                        Visibility="Collapsed"/>
                <Button x:Name="NextBtn"
                        Content="Next ▶"
                        Padding="10,4"
                        Click="Next_Click"/>
            </StackPanel>

        </StackPanel>
    </Border>
</Window>
